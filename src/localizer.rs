use crate::hardware_data_manager::Update;
use crate::Point;

pub fn localize_points(_measurements: &[Update]) -> Vec<Point> {
    todo!() // ISSUE 34

    // vec![Point::new(1.3, 4.746)]
    // For fun before you start @Skylar @Tyler: comment out todo!, uncomment the above, and cargo run
    // hint try resizing the terminal :D
}
